title: Codebots
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://codebots.com"
destination: ./site

plugins:
  - jekyll-feed

include: ['_pages']

defaults:
  - scope:
      path: "assets/img"
    values:
      image: true